# Uncomment the next line to define a global platform for your project
platform :macos, '12.1'

# Do not use frameworks for command line tool
#use_frameworks!

target 'HelloWorld' do
  # Pods for HelloWorld
  pod 'Alamofire', '5.5.0'

end
